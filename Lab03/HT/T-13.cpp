#include <iostream>
using namespace std;

main()
{
    int sum;
    int num;

    cout << "Enter 5 integers: \n";
    cin >> num;

    sum = num + sum;
    cin >> num;

    sum = num + sum;
    cin >> num;

    sum = num + sum;
    cin >> num;

    sum = num + sum;
    cin >> num;

    sum = num + sum;

    cout << "Sum = " << sum;
}